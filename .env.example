# Backend API URL (only needed if NOT using vite proxy, e.g. in production)
# During dev, Vite proxies /api → http://localhost:8000 automatically.
# For production, set this to your Railway backend URL:
# VITE_API_URL=https://your-backend.up.railway.app/api
